const walletAbi = require("./walletAbi.json")
const walletContractAddresses = require("./walletContractAddresses.json")
const erc20Abi = require("./erc20Abi.json")
const nftAbi = require("./nftAbi.json")

module.exports = {
    walletAbi,
    walletContractAddresses,
    erc20Abi,
    nftAbi,
}
